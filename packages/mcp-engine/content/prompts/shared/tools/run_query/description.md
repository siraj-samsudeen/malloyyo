Run ONE read-only SQL statement (SELECT/WITH/FROM; the dialect of the model's own database) directly on the model's connection. Available only because this model explicitly enables it. The model's guidance topics (yo_help) define the canonical tables, columns, and rules — read the relevant topic BEFORE writing SQL, and prefer the model's published sources (`query`) when they already answer the question. One statement per call; anything that writes is rejected.
